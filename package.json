{
  "name": "socialrisex-main",
  "version": "1.0.0",
  "description": "Social media platform for earning money",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cd backend && npm run dev",
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.9.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "memory-cache": "^0.2.0",
    "mongoose": "^7.0.3",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.1"
  },
  "engines": {
    "node": "20.x"
  }
}
